[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn --chdir backend --worker-class gthread --workers 1 --threads 4 --timeout 120 app:app"
healthcheckPath = "/api/pipeline/status"
healthcheckTimeout = 600
restartPolicyType = "on_failure"
